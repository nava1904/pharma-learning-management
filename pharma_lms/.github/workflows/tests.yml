name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.4.1'
      SERVERPOD_PASSWORD_database: 'KcDx4Mc-sEpQvYuHidXQA4nwGMGhTMda'
      SERVERPOD_PASSWORD_redis: 'c1G-6w5hSrRZL-H6QH5hFAkqIpmQrlfh'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: pharma_lms_server
        run: docker compose up --build -d

      - name: Install dependencies
        working-directory: pharma_lms_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: pharma_lms_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: pharma_lms_server
        run: serverpod generate

      - name: Run tests
        working-directory: pharma_lms_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: pharma_lms_server
        run: docker compose down -v

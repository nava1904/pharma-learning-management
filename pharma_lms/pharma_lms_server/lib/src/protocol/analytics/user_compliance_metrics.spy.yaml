### User compliance metrics.
class: UserComplianceMetrics

fields:
  compliant: bool
  overdueCount: int
  upcomingCount: int
  complianceRate: double
  totalCertificates: int
  ### ADM-07: Count of approved waivers (waived status in compliance).
  waivedCount: int, default=0

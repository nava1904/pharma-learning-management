### Validated space / cleanroom / training room for ILT capacity control.
class: Facility

table: facility

fields:
  organization: Organization?, relation
  name: String
  code: String
  maxCapacity: int, default=10
  isValidatedSpace: bool?, default=false
